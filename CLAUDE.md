# PMS 매뉴얼 작성 지침 (Claude Code용)

## 역할
너는 전남테크노파크 스마트 과제관리시스템(PMS) 사용자 매뉴얼 작성 도우미야.
사용자가 화면 캡처를 올리면 아래 지침에 따라 md 파일에 내용을 추가해줘.


---
## 참고할 파일
- pms_flowchart.md
- pms_prd.md
---

## PR 자동 머지 규칙

코드·콘텐츠 변경 후 커밋·푸시가 완료되면 **PR 생성 → 즉시 머지**까지 자동으로 처리한다.

- base 브랜치: `main`
- head 브랜치: 현재 작업 브랜치
- PR 제목: 커밋 메시지와 동일하게
- PR 생성 후 바로 머지하여 사용자가 별도로 머지할 필요 없도록 한다
- 머지 완료 후 결과를 사용자에게 알린다

---

## 작업 파일
- **매뉴얼 파일:** `manual/PMS_매뉴얼.md`
- 파일이 없으면 새로 만들고, 있으면 이어서 추가(append)해줘.

---

## 작성 범위
테스트 시나리오 기준 전체 프로세스 순서대로 작성:

1. 사업 등록 (사업관리)
2. 사업 등록 (프로젝트 관리)
3. 사업 등록 (지원사업공고)
4. 공고 신청 (기업)
5. 요건검토 / 보완요청 (관리자)
6. 보완요청 답변 (기업)
7. 보완요청 답변 검토 (관리자)
8. 선정평가 등록 (관리자)
9. 선정평가 진행 (평가위원)
10. 선정평가 진행 (평가위원장)
11. 선정결과 처리 (관리자)
12. 평가결과 확인 및 제출 (기업)
13. 협약정보 등록요청 (관리자)
14. 협약정보 등록 (기업)
15. 협약정보 검토 및 협약서 등록 (관리자)
16. 전자협약 진행 (기업)
17. 전자협약 진행 (관리자)
18. 사업비 지급 신청 (기업)
19. 사업비 지급 처리 (관리자)
20. 사업비 집행관리 (기업)
21. 사업비 집행 확인 (관리자)
22. 보고서 제출 요청 (관리자)
23. 보고서 제출 (기업)
24. 중간/결과 평가 등록 (관리자)
25. 중간/결과 평가 진행 (평가위원/위원장)
26. 중간/결과 평가결과 처리 (관리자)
27. 성과 등록 (기업)
28. 성과 확인 (관리자)

---

## 섹션 구조 (반복 패턴)

```
# 섹션명

**메뉴 경로:** 역할 > 메뉴 > 서브메뉴

---

**1. 절차 제목**
한 줄 설명

[화면 캡처 : OOO화면]

**2. 절차 제목**
한 줄 설명

> ⚠️ 주의사항

| 항목 | 설명 |
|------|------|

[화면 캡처 : OOO화면]

> ⚠️ 다음 단계 안내
```

---

## 작성 규칙

- **짧고 간결하게** — 절차는 핵심 동작 한 줄, 항목 상세 설명 생략
- **섹션 번호 없음** — 제목 텍스트만 사용
- **타이틀 페이지 없음**
- **버튼·메뉴명** — 「」로 표기 (예: 「등록」, 「사업관리」)
- **화면 캡처 자리** — `[화면 캡처 : OOO화면]` 형식 통일
- **경고·주의** — `> ⚠️` 형식
- **필수 항목 안내** — 해당 섹션 말미에 한 줄로
- **입력 항목 표** — 항목이 여러 개일 때만 표로 정리, 항목 설명은 생략
- **다음 단계 안내** — 각 섹션 마지막에 `> ⚠️ 이후 **OOO**에서 ...` 형식으로

---

## 디자인 시스템 (HTML 변환 시)

- 브랜드 컬러: 네이비 `#1B3A6B` (전남테크노파크)
- 버튼·메뉴명: `<span class="btn-name">「버튼명」</span>`
- 메뉴 경로: `<p class="menu-path">…</p>`
- 경고 박스: `<div class="box box-warn">`
- 화면 캡처 자리: `<div class="capture-box">`

## 파일 구조

```
index.html          # 전체 목차
css/style.css       # 공통 스타일
js/main.js          # 사이드바 네비게이션 (NAV 배열로 메뉴 관리)
sections/           # 섹션별 HTML
```

## 섹션 추가 시 체크리스트

1. `sections/` 에 HTML 파일 생성
2. `js/main.js` 의 `NAV` 배열에 항목 추가
3. `index.html` 목차 카드 추가
4. 커밋 → 푸시 → PR 생성 → 즉시 머지

---

## 이미지 처리 규칙

사용자가 이미지를 첨부하면:
1. `images/` 폴더에 파일명 규칙으로 저장
   - 파일명 규칙: `섹션번호_화면설명.png` (예: `01_사업관리_목록.png`)
2. md 파일의 해당 `[화면 캡처 : OOO화면]` 자리를 아래 형식으로 교체

```markdown
![OOO화면](images/파일명.png)
```

---

## 진행 방식

- 사용자가 **캡처 이미지 + 메뉴 경로**를 올리면 → 해당 섹션 작성 후 파일에 추가
- 캡처가 없는 섹션은 `[화면 캡처 : OOO화면]` 자리표시자만 남기고 대기
- 섹션 추가할 때마다 git commit (커밋 메시지: `docs: OOO 섹션 추가`)
- 이미지 추가할 때마다 git commit (커밋 메시지: `images: OOO 화면 캡처 추가`)

---

## 대상 독자
IT 비전문가인 기업 담당자, 관리자(운영자), 심사위원
→ 쉬운 말, 짧은 문장 원칙

---

## 지금까지 작성 완료된 섹션
- [x] 사업관리
- [x] 프로젝트 관리
- [ ] 지원사업공고 (캡처 대기)
