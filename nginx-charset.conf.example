# Nginx 서버를 사용 중이라면, 아래 내용을 server { ... } 블록 안에 추가하세요.
# (경로: /etc/nginx/sites-available/해당설정파일)
#
# charset utf-8;
# charset_types text/html text/css application/javascript text/plain;
#
# 추가 후 아래 명령으로 반영:
#   sudo nginx -t && sudo systemctl reload nginx
